# Database (na Railway zmeň na /data/prod.db)
DATABASE_URL="file:/data/prod.db"

# JWT Secret - vygeneruj silný reťazec, napr.: openssl rand -base64 32
JWT_SECRET="zmen-toto-na-silny-nahodny-retazec"

# Prostredie
NODE_ENV="production"
